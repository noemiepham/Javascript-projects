document.addEventListener('DOMContentLoaded', function () {
      let status = true;
      let st = true;
      let menu = document.querySelector('.menu');
      let chungtoi = document.querySelector('.chungtoi');
      let positionTop = chungtoi.offsetTop;
      let positionBottom = chungtoi.offsetTop + 600;
      let objectLoading = document.querySelectorAll('.s3');
      let stLoading = true;
      let positionLoading = objectLoading.offsetTop - 300;
      

      window.addEventListener('scroll', function () {
        console.log('day la chuot');
        
          })
     }, false);